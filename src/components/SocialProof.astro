---
const logos = [
  "Acme Corp", "TechFlow", "Globex", "Initech", "Hooli", "Pied Piper",
];

const testimonials = [
  {
    quote: "FlowStack replaced three different tools for us. Our team velocity increased 40% in the first month alone.",
    name: "Sarah Chen",
    title: "VP of Engineering, TechFlow",
    avatar: "SC",
  },
  {
    quote: "The automation features are incredible. We've saved roughly 15 hours per week on project management overhead.",
    name: "Marcus Rodriguez",
    title: "CTO, Globex Industries",
    avatar: "MR",
  },
  {
    quote: "Finally, a PM tool that developers actually want to use. The GitHub integration is best-in-class.",
    name: "Alex Kim",
    title: "Engineering Lead, Initech",
    avatar: "AK",
  },
];
---

<section class="relative py-24 md:py-32">
  <div class="mx-auto max-w-6xl px-6">
    <!-- Logo bar -->
    <div class="text-center">
      <p class="text-sm font-medium uppercase tracking-wider text-surface-500">
        Trusted by teams at
      </p>
      <div class="mt-8 flex flex-wrap items-center justify-center gap-x-12 gap-y-6">
        {logos.map((name) => (
          <div class="flex h-8 items-center rounded bg-surface-800/50 px-6 text-sm font-semibold text-surface-500">
            {name}
          </div>
        ))}
      </div>
    </div>

    <!-- Testimonials -->
    <div class="mt-20 grid gap-6 md:grid-cols-3">
      {testimonials.map((t) => (
        <div class="rounded-2xl border border-white/5 bg-white/[0.02] p-8">
          <div class="mb-4 flex gap-1">
            {[...Array(5)].map(() => (
              <svg class="h-4 w-4 text-amber-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            ))}
          </div>
          <p class="text-surface-300 leading-relaxed">"{t.quote}"</p>
          <div class="mt-6 flex items-center gap-3">
            <div class="flex h-10 w-10 items-center justify-center rounded-full bg-accent-600/20 text-sm font-semibold text-accent-400">
              {t.avatar}
            </div>
            <div>
              <p class="text-sm font-medium text-white">{t.name}</p>
              <p class="text-xs text-surface-500">{t.title}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
